apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: apps-prod
  namespace: flux-system
spec:
  interval: 5m
  resources:
    - ../infrastructure/controllers/dashboard.yaml  # Wskazanie pliku deployment
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
