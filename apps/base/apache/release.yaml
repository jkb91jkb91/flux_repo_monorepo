apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: apache
  namespace: staging
spec:
  interval: 1m
  chart:
    spec:
      chart: ./apps/base/apache/apache_HR/grafana
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      interval: 1m
